# Use this repo as zsh Custom folder
ln -si $(pwd)/zsh/.zshrc ~/.zshrc
ln -si $(pwd)/git/common.config ~/.gitconfig
ln -si $(pwd)/git/personal.config ~/personal.gitconfig
ln -si $(pwd)/git/work.config ~/work.gitconfig
ln -si $(pwd)/git/.gitignore_global ~/.gitignore_global
